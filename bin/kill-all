#!/bin/bash
cd `realpath "$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"`
if [[ -f joiner.pid ]]; then 
  kill -SIGINT `cat joiner.pid`
fi
if [[ -f model-server.pid ]]; then 
  kill -SIGINT `cat model-server.pid`
fi
if [[ -f page-server.pid ]]; then
  kill -SIGINT `cat page-server.pid`
fi
