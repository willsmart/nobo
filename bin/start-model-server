#!/bin/bash
args=$@
bindir=`realpath "$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"`

cd "$bindir/../tools/common-js"
npm install

cd "$bindir/../tools/model-server"
npm install

cd "$bindir/.."
node tools/model-server/main.js $args
