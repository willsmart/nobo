#!/bin/bash
args=$@
bindir=`realpath "$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"`
toolsdir="$bindir/../tools"

cd "$bindir/.."
node "$toolsdir/js/mains/db/update-db-templates.js" $args
